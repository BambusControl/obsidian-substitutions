import {TextReplacement} from "../type/textReplacement";

/* TODO: Remove constant replacement with user set substitutions */
export const substitutionValue: TextReplacement = {
    from: "-->",
    to: "→",
    // from: "(r)",
    // to: "→",
}
